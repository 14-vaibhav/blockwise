[project]
name = "blockwise"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "numpy",
    "scipy",
    "requests",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "api:app"

[tool.vercel.scripts]
build = "python build_frontend.py"
